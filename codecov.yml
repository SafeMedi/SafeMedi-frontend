comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false

coverage:
  precision: 2
  round: down
  range: "0...100"
  status:
    project:
      default:
        target: 60%
        threshold: 0%
    patch:
      default:
        target: 85%
        threshold: 2%
